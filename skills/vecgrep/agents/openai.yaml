interface:
  display_name: "vecgrep"
  short_description: "Semantic code search — find code by meaning, not just text"
  default_prompt: "Use $vecgrep to search codebases by meaning. Use it when you need to find relevant code, similar patterns, or explore an unfamiliar codebase."
